using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Crafting : MonoBehaviour
{
    //when an item is collected add an item int and set item in inventory bool to true
    //in crafting room have a tabel/boxes for items 
    // transform slots in each of these boxes 
    // as collecting if nothing is in first slot then put item in that slot 
    //if there is something in that slot move to next and keep checking if the prevoise slots are empty.
    // sewing machine cnsists of 3 item input boxes and one box that is for the result
    //drag item from inventory to one of the slots --> on trigger slot snap to transform 
    //item int -=1 
    // if item int == 0 delete item sprite from inventory (do I want to move items in inventory up to the new empty slot???)
    // make a table of 





    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
